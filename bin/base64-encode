#!/bin/bash

source ~/secret-project-331/bin/common-functions
set -euo pipefail

echo Type the string you want to base64 encode and press enter:
read -r input

run_command eval "echo -n '$input' | base64 -w 0"
