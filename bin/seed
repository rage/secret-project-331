#!/bin/bash
set -euo pipefail

source ~/secret-project-331/bin/common-functions

POD_NAME=$(kubectl get pods -l app=headless-lms -o name | head -n 1)

run_command kubectl exec -it "$POD_NAME" "$@" -- cargo run --bin seed "$@"
