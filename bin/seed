#!/bin/bash
COMMAND="psql"
if [ ! -f ../headless-lms/.env ]
then
  export $(cat headless-lms/.env | xargs)
fi
ARGUMENTS=($DATABASE_URL --file headless-lms/db/seed.sql "$@")

echo "> $COMMAND" "$(printf "%q " "${ARGUMENTS[@]}")"
exec "$COMMAND" "${ARGUMENTS[@]}"

