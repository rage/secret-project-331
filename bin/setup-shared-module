#!/bin/bash
set -euo pipefail
source "$(dirname "$0")/.common"

BASEDIR="$(dirname "${BASH_SOURCE[0]}")"
FILE_PATH="$BASEDIR/../shared-module/setup-shared-module.ts"
RELATIVE_PATH=$(realpath --relative-to="$(pwd)" "$FILE_PATH")

run_command npx ts-node  -O '{"module":"commonjs"}' "$RELATIVE_PATH"
