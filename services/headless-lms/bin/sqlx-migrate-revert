#!/bin/bash
set -euo pipefail

COMMAND="cargo"
ARGUMENTS=(sqlx migrate revert "$@" -- --bin headless-lms-actix)

echo "> $COMMAND" "$(printf "%q " "${ARGUMENTS[@]}")"
exec "$COMMAND" "${ARGUMENTS[@]}"
