console.log(
  // eslint-disable-next-line i18next/no-literal-string
  "Running npm run postinstall on the repo root whever a file changes in shared-module/src",
)

// eslint-disable-next-line @typescript-eslint/no-empty-function
setInterval(() => {}, 50000)
