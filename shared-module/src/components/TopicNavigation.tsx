import styled from "@emotion/styled"
import React from "react"

const TopicNavigation = () => {
  // eslint-disable-next-line i18next/no-literal-string
  return <div>{`This is a side-navigation`}</div>
}

export default TopicNavigation
