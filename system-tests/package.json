{
  "name": "system-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --browser=chromium",
    "spec": "npx playwright test -c src/tests --browser=chromium",
    "help": "playwright --help",
    "record-test": "npx playwright codegen project-331.local",
    "record-test-admin": "npm run create-login-states && npx playwright codegen --load-storage=src/states/admin@example.com.json project-331.local",
    "record-test-teacher": "npm run create-login-states && npx playwright codegen --load-storage=src/states/teacher.json project-331.local",
    "record-test-user": "npm run create-login-states && npx playwright codegen --load-storage=src/states/user.json project-331.local",
    "create-login-states": "npx playwright test --browser=chromium src/setup/",
    "test-debug": "env DEBUG=pw:api npm run test",
    "update-snapshots": "npx playwright test --update-snapshots --browser=chromium"
  },
  "keywords": [],
  "devDependencies": {
    "@playwright/test": "^1.12.3",
    "playwright": "^1.12.3",
    "typescript": "^4.3.5"
  }
}
