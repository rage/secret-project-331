import { test } from "@playwright/test"

/** Owns student numbers `9000003xx`. */
test.fixme("A student with no usable enrolment is told to enrol, and recovers after enrolling", () => {
  // Waiting on enrolment resolution.
})

test.fixme("Enrolment selection prefers the degree enrolment over the open university one", () => {
  // Waiting on enrolment resolution. Assert against the mock's call log, not the UI.
})
