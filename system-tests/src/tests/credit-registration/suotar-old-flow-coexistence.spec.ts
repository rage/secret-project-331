import { test } from "@playwright/test"

/**
 * Owns student numbers `9000010xx` and the `credit-registration-old-flow` course. Must be
 * green before the first real course is cut over.
 */
test.fixme("A course left on the old flow keeps registering through the legacy pull API", () => {
  // Waiting on the guard.
})

test.fixme("A Suotar-enabled module leaves the pull stream and mirrors its successes into the legacy ledger", () => {
  // Waiting on the guard.
})
